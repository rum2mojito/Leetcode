class Solution {
public:
    vector<pair<int, int>> reconstructQueue(vector<pair<int, int>>& people) {
        vector<vector<int>> res(people.size(),vector<int>(0);
        for (int i=0;i<people.size();i++) {
            res
        }
    }
};
